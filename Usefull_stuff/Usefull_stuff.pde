// Array Lists

ArrayList <box>  boxes;
//
 boxes = new ArrayList<box>();
 //
 
  for (box bx : boxes) {
    bx.display();
  }
   if (mousePressed) {
    boxes.add(new box(mouseX, mouseY));
  }
  
  
  
  
